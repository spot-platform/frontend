import type { Metadata } from 'next';

export const metadata: Metadata = { title: '스팟 등록' };

export default function PostPage() {
    return (
        <main>
            <h1>스팟 등록</h1>
            {/* TODO: SpotForm component */}
        </main>
    );
}
